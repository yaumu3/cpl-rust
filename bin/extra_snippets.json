{
  "Debug": {
    "prefix": "dbg",
    "body": "dbg!($0);"
  },
  "Iterable to char separated String": {
    "prefix": "sep",
    "body": "$1.iter().map(|x| x.to_string()).collect::<Vec<_>>().join(\"$2\");"
  }
}
